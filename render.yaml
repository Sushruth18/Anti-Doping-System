services:
  - type: web
    name: anti-doping-backend
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    # Render's disk is ephemeral (wiped on every redeploy AND every
    # restart/wake from spin-down), so re-seeding from committed
    # /data/*.json has to happen on every container boot, not just once
    # at deploy time — chained into startCommand (runs every start) rather
    # than a one-shot preDeployCommand (runs only on deploy). `&&` means a
    # seeding failure stops the boot instead of silently serving an empty
    # DB. Render injects $PORT at runtime; uvicorn must bind to it rather
    # than a hardcoded port.
    # backfill_anomalies.py runs right after seed.py for the same reason:
    # seed.py wipes the anomalies table on every reseed (see its comment),
    # so anomalies must be repopulated every boot too, not just scored
    # lazily as new samples trickle in via POST. It's idempotent, so
    # re-running it on every restart (even without a reseed) is safe.
    startCommand: python app/db/seed.py && python app/db/backfill_anomalies.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11
      - key: CORS_ORIGINS
        value: https://anti-doping-system.vercel.app
