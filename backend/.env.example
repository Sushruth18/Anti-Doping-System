DATABASE_URL=sqlite:///./app.db

# Comma-separated list of allowed CORS origins. If unset, falls back to
# localhost:5173 + the production Vercel domain (see app/main.py).
CORS_ORIGINS=http://localhost:5173,https://anti-doping-system.vercel.app
